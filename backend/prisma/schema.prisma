generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
}

model Aviso {
  id          Int    @id @default(autoincrement())
  title       String
  description String
  type        String
}