generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url = env("DATABASE_URL")
}

model Aviso {
  id          Int    @id @default(autoincrement())
  title       String
  description String
  type        String
}

model Edital {
  id          Int    @id @default(autoincrement())
  titulo      String
  descricao   String
  data        String
  status      String
  link        String
}